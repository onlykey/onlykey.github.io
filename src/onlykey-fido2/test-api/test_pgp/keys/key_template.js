module.exports = (function() {
  var keyDescription = {
    Service: false,// keybase or protonmail
    FingerPrint: "",//uppsercase+no spaces
    KeySize: "0",// 0 !== false
    PrivKeyPW: "test123",//key passphrase to unlock
    PrivKey: (``),//turn to false for services
    PubKey: (``)//pubkey
  };

  return keyDescription;
})();
